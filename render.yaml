# render.yaml
services:
  - type: worker
    name: email-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python simple_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
    plan: free
